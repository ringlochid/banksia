# AutoClaw does not load .env files implicitly. Export selected values into the
# process environment, or place them in an explicit service environment file.
# AUTOCLAW_ nested keys use double underscores.

AUTOCLAW_ENV=development
AUTOCLAW_DEBUG=true

# Postgres from docker-compose (host port 5433). Omit to fall back to local SQLite.
AUTOCLAW_DATABASE_URL=postgresql+asyncpg://autoclaw:autoclaw@localhost:5433/autoclaw

# OpenClaw gateway
AUTOCLAW_OPENCLAW__ENABLED=false
AUTOCLAW_OPENCLAW__GATEWAY_URL=ws://127.0.0.1:18789
AUTOCLAW_OPENCLAW__GATEWAY_PROFILE=default
